---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: opticaldevices.hardware.ran.example.com
spec:
  group: hardware.ran.example.com
  names:
    kind: OpticalDevice
    listKind: OpticalDeviceList
    plural: opticaldevices
    singular: opticaldevice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions.status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions.status
      name: READY
      type: string
    - jsonPath: .spec.parameters.bandwidth
      name: BANDWIDTH
      type: string
    - jsonPath: .spec.parameters.laserPower
      name: POWER
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpticalDevice is the Schema for the opticaldevices API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpticalDeviceSpec defines the desired state of OpticalDevice
            properties:
              controllerConfig:
                description: Connection configuration for the local controller.
                properties:
                  hostname:
                    description: Hostname or IP address of the local controller PC.
                    type: string
                  port:
                    description: Port of the API service (e.g., NI-VISA wrapper) on
                      the local controller.
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - hostname
                - port
                type: object
              location:
                description: Location of the device.
                type: string
              parameters:
                description: Desired hardware parameters.
                properties:
                  bandwidth:
                    description: The desired bandwidth, e.g., "100Gbps".
                    type: string
                  channel:
                    description: Optional optical channel number.
                    type: integer
                  laserPower:
                    description: The desired laser power, e.g., "14.5dBm".
                    type: string
                required:
                - bandwidth
                - laserPower
                type: object
            required:
            - controllerConfig
            - location
            - parameters
            type: object
          status:
            description: OpticalDeviceStatus defines the observed state of OpticalDevice
            properties:
              conditions:
                description: Represents the latest available observations of a OpticalDevice's
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is the timestamp of the last successful
                  reconciliation.
                format: date-time
                type: string
              observedBandwidth:
                description: ObservedBandwidth reflects the last known bandwidth from
                  the hardware.
                type: string
              observedLaserPower:
                description: ObservedLaserPower reflects the last known laser power
                  from the hardware.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
